# Example environment configuration for docker-compose / make targets.
# Copy to .env and adjust as needed (.env is gitignored).

# Host port for the dev MariaDB (docker-compose.yml).
DB_PORT=3306

# Host port for the minimal test DB (docker-compose.test.yml / make test).
TEST_DB_PORT=3307
